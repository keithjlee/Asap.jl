# Loads and springs

Nodal and element loads, the fixed-end-force engine, and nodal spring supports.

```@autodocs
Modules = [Asap]
Pages = ["src/Loads/loads.jl", "Elements/kernels/fixed_end_forces.jl", "Springs/nodal_springs.jl"]
Private = false
```
