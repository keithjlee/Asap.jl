# Historical note: during Phase 1 the new core was developed "dark" — these
# files were wrapped in a scratch module (AsapNext) alongside the legacy
# code. Since the module flip, the new core IS Asap; the alias keeps the
# test files unchanged.

const AsapNext = Asap
